# devcontainers

Docker container definitions for development

## Building

### Dependencies

* docker
* make

### Usage

Run `make image-archlinux` to build the `zachorosz/devcontainer:archlinux` image.
Then run `docker run --rm -it zachorosz/devcontainer:archlinux`.